Patch notes:
- Added :root CSS variables for a coherent theme
- Updated `body` background to use gradient variable
- Created `.btn` and `.btn-primary` styles
- Polished `.tool-card` buttons and hover states
- Kept previous input/placeholder changes

Files patched: styles.css, tools/style.css, assests/css/styles.css
